This class accepts the start and end limit for the X variable, the mathematical equation for the desiured functions and draws the function diagram as a Jpeg file. That image can be duirctly saved via the browser.


A sample PHP file is added which enables quick testing on the class.


NOTE: GD/GD2 must be installed on the server.(enable GD2 extension
in you php.ini file .)

NOTE: f(x) must be written in the php sysntax such as : sin(2*x); 
ex: x*x;
ex: 2*x*x+3*x+1;